{
  "name": "Live Transcript Worker (Direct CUDA)",
  "image": "nvidia/cuda:12.9.1-cudnn-runtime-ubuntu24.04",
  "runArgs": [
    "--gpus", "all",
    "--name", "live_transcript_dev"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ],
      "settings": {
        "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
        "python.analysis.extraPaths": ["${workspaceFolder}/src"]
      }
    }
  },
  "remoteUser": "root",

  "updateContentCommand": "export DEBIAN_FRONTEND=noninteractive && apt-get update && apt-get install -y python3 python3-pip python3.12-venv ffmpeg curl unzip git && export DENO_INSTALL=/usr/local && curl -fsSL https://deno.land/install.sh | sh && bash scripts/setup.sh"
}