{
  "name": "Live Transcript Worker (Direct CUDA)",
  "image": "nvidia/cuda:12.9.1-cudnn-runtime-ubuntu24.04",
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {
      "username": "vscode",
      "userUid": "1000",
      "userGid": "1000",
      "upgradePackages": true
    }
  },
  "runArgs": [
    "--gpus", "all",
    "--name", "live_transcript_dev"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ],
      "settings": {
        "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
        "python.analysis.extraPaths": ["${workspaceFolder}/src"]
      }
    }
  },
  "remoteUser": "vscode",
  "updateContentCommand": "sudo apt-get update && sudo DEBIAN_FRONTEND=noninteractive apt-get install -y python3 python3-pip python3.12-venv ffmpeg curl unzip git && curl -fsSL https://deno.land/install.sh | sudo DENO_INSTALL=/usr/local sh && bash scripts/setup.sh"
}